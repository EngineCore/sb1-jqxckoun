---
interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<li class="card">
  <a href={href} class="block p-6 bg-white dark:bg-gray-800 rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300">
    <h2 class="text-xl font-bold mb-2">
      {title}
      <span class="inline-block transition-transform duration-300 group-hover:translate-x-1">→</span>
    </h2>
    <p class="text-gray-600 dark:text-gray-300">
      {body}
    </p>
  </a>
</li>